import numpy as np
import tensorflow as tf






def sphere(x,y,z,s):
    return s-tf.sqrt(tf.pow(x,2)+tf.pow(y,2)+tf.pow(z,2))


def sphere(x,y,z,s):
    return s-tf.sqrt(tf.pow(x,2)+tf.pow(y,2)+tf.pow(z,2))

